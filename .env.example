# Optional development credentials. Names only — never put real values here.
# A missing variable disables only the adapter that needs it; nothing fails at startup.

GITHUB_TOKEN=
WEB_SEARCH_API_KEY=
HUNTER_LLM_BASE_URL=
HUNTER_LLM_API_KEY=
HUNTER_LLM_MODEL=
HUNTER_LLM_TIMEOUT_SECONDS=
HUNTER_LLM_MAX_RESPONSE_BYTES=
